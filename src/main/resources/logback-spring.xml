<?xml version="1.0" encoding="UTF-8"?>
<!--
    Configuração de logging por perfil (issue #94 — observabilidade):
    - dev/default: texto legível no console, incluindo o correlation-id ([requestId]).
    - prod: uma linha JSON por evento (logstash-logback-encoder), com o requestId
      (e demais campos do MDC) promovido a chave de topo, pronto para ingestão.

    O correlation-id é colocado no MDC pelo CorrelationIdFilter sob a chave "requestId".
-->
<configuration>

    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="CarlessoPilatesApi"/>

    <!-- Padrão de console legível: data, nível, thread, [requestId], logger, mensagem. -->
    <property name="CONSOLE_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%X{requestId:-}] %logger{36} - %msg%n"/>

    <springProfile name="prod">
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>requestId</includeMdcKeyName>
                <customFields>{"app":"${appName}"}</customFields>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>

    <springProfile name="!prod">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>${CONSOLE_PATTERN}</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

</configuration>
